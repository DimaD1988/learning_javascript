"use strict";
/*
class GuessingGame {
	constructor() {}
	setRange(min, max) {
		let r;
		this.min = min;
		this.max = max;
		this.guess(this.min, this.max);
	}
	guess() {
		this.r = Math.ceil((this.min + this.max) / 2);
		console.log(this.r);
		return this.r;
	}
	lower() {
		return (this.max = this.r);
	}
	greater() {
		return (this.min = this.r);
	}
}
const number = 286;
const game = new GuessingGame();
game.setRange(0, 1501);

let result = game.guess();
game.lower();
result = game.guess();
game.lower();
result = game.guess();
game.greater();
result = game.guess();
game.greater();
result = game.guess();
game.lower();
result = game.guess();
game.lower();
result = game.guess();
game.lower();
result = game.guess();
game.lower();
result = game.guess();
game.greater();
result = game.guess();
game.lower();
result = game.guess();
console.log(result, number);
*/
