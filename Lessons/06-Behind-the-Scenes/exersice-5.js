"use strict";
/*
function brightest(colors) {
	const arr = [];
	colors.forEach((v) => {
		for (let i = 1; i < 7; i = i + 2) {
			arr.push(parseInt(v.slice(i, i + 2), 16));
		}
	});
	const maxValueIx = arr.indexOf(Math.max(...arr));
	return colors[Math.floor(maxValueIx / 3)];
}
 */

/*
function disemvowel(str) {
  return str.replace(/[aeiou]/gi, '');
}
*/
/*
function isIsogram(str){
	return new Set(str.toUpperCase()).size == str.length;
}
*/
/*
function explode(s) {
	let explodeStr = "";
	for (let i = 0; i < s.length; i++) {
		explodeStr += s[i].repeat(Number(s[i]));
	}
	return explodeStr;
}
*/
/*
function getParticipants(handshakes){
  let farmers = 0
  while(handshakes > farmers * (farmers - 1) / 2){
    farmers++
  }
  return farmers
}
*/
