'use strict';
// Create pangram
/*
const isPangram = sentence => {
  let lCased = sentence.toLowerCase();
  for (let char of 'abcdefghijklmnopqrstuvwxyz') {
    // if (lCased.indexOf(char) === -1) {
    if (!lCased.includes(char)) {
      return false;
    }
  }
  return true;
};
// console.log(isPangram(`The five boxing wizards jump quickly`));
// isPangram(`The five boxing wizards jump quickl`);
*/
