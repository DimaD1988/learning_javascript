'use strict';
/*
const test2 = [200, 450, -400, 3000, -650, -130, 70, 1300];

const totalDepositsUSD = test2
  .filter(mov => mov > 0)
  .map(mov => mov * 1.1)
  .reduce((acc, mov) => acc + mov, 0);
// console.log(test2, totalDepositsUSD);

const firstWithdrawal = test2.find(mov => mov < 0);
console.log(test2, firstWithdrawal);
*/
